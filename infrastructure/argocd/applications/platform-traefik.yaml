apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://traefik.github.io/charts
      chart: traefik
      targetRevision: 40.0.0
      helm:
        valueFiles:
          - $values/infrastructure/platform/traefik/values.yaml
    - repoURL: https://github.com/HoanhHop11/JB_Calling_translation_system.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      # Replace thay vì strategic-merge-patch để tránh duplicate port khi đổi containerPort
      - Replace=true
